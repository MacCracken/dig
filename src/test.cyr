# dig — top-level test entry (referenced by cyrius.cyml [build].test).
# For unit tests, prefer adding cases to tests/dig.tcyr.

include "lib/syscalls.cyr"

# Stub entry for `cyrius.cyml [build].test`. Deliberately empty: the real suite
# is `tests/dig.tcyr`, which `cyrius test` auto-discovers alongside this file.
# Kept because the manifest's [build].test key requires a target to exist.
fn main() {
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
